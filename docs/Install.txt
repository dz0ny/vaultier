Clone
====
cd /www
mkdir /www/vaultier
cd vaultier
git clone git@git.rclick.cz:rclick/vaultier.git .

Install required packages
==================
sudo apt-get install postgresql postgresql-contrib
sudo apt-get install python
sudo apt-get install python-dev
sudo apt-get install python-pip
sudo pip install virtualenv


Create venv
========
virtualenv ./env
source ./env/bin/activate
pip install -r sources/REQUIREMENTS

Prepare database
=============
su - postgres
psql
create user vaultier with password 'vaultier'
create database vaultier owner vaultier
CTRL+] or \q



Configure nginx
===========
cd /etc/nginx/sites-enabled/
sudo ln -s /www/vaultier/cfg/nginx.conf /etc/nginx/sites-enabled/vaultier
sudo chmod 0777 /var/www/vaultier/run/vaultier.sock
chmod -R 755 /www/vaultier/sources/static


Start vaultier
=========